package com.jaidev.seeaplayer

import android.content.Context

class videoItemPreferences(private val context: Context) {

//    private val prefs: SharedPreferences by lazy {
//        context.getSharedPreferences("file_item_prefs", Context.MODE_PRIVATE)
//    }
//
//    // Save updated FileItem to SharedPreferences
//    fun saveFileItem(videoData: VideoData) {
//        val key = VideoData.getPrefKey(videoData)
//        prefs.edit().putString(key, videoData.originalFileName).apply()
//    }
//
//    // Retrieve FileItem's originalFileName from SharedPreferences
//    fun getOriginalFileName(fileItem: VideoData): String {
//        val key = VideoData.getPrefKey(fileItem)
//        return prefs.getString(key, fileItem.originalFileName) ?: fileItem.originalFileName
//    }
}
