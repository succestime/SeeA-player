<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">

    <!-- Background shape with rounded corners on top-right and bottom-right -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners
                android:topRightRadius="9dp"
                android:bottomRightRadius="9dp"
                android:topLeftRadius="0dp"
                android:bottomLeftRadius="0dp" /> <!-- Only right side corners are curved -->
        </shape>
    </item>

    <!-- Ripple effect shape with rounded corners on top-right and bottom-right -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="?attr/colorControlHighlight" />
            <corners
                android:topRightRadius="9dp"
                android:bottomRightRadius="9dp"
                android:topLeftRadius="0dp"
                android:bottomLeftRadius="0dp" /> <!-- Only right side corners are curved -->
        </shape>
    </item>

</ripple>
